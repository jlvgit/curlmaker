
<% title = JSON.parse(@curl.service.gsub('=>', ':')) %>
<h3><%= title['service'] + " - " + @curl.name %></h3>

<hr>

<div class="row">

  <div class="col-xs-4">
    <%= render 'data_forms' %>
  </div>

  <div class="col-xs-8">
    <pre contenteditable="true"><%= create_curl_text(@curl, params) %> <span id="gallery-box">| python -m json.tool</span> </pre>
    <label><input type="checkbox" name="post-format" id="post-format-gallery">Pretty output?</label>
  </div>

</div>
